{% extends 'baseAdmin.html.twig' %}

{% block title %}Option{% endblock %}

{% block contentAdmin %}
   <div class="container mt-5">
        <div class="row">
<h1>Option</h1>

<table class="table">
	<tbody>
		<tr>
			<th>Id</th>
			<td>{{ option.id }}</td>
		</tr>
		<tr>
			<th>Name</th>
			<td>{{ option.name }}</td>
		</tr>
	</tbody>
</table>

<a href="{{ path('appadmin__option_index') }}">back to list</a>

<a href="{{ path('app_admin_option_edit', {'id': option.id}) }}" class="btn btn-primary">edit</a>

{{ include('option/_delete_form.html.twig') }}

        </div>
   </div>
{% endblock %}
